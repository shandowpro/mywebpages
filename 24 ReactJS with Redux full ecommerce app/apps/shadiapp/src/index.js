
// Importing the main librarires of ReactJS :
import React from "react";
import ReactDOM  from "react-dom";

// Importingm the main  App component file to be used in the rendering mehtod : 
import App from "./App";

// Rendering the [App] main component inside the ReactDom :
ReactDOM.render(  <App/>  , document.getElementById('root') ) ;